# Core Demo

Framework-neutral demo for the initial Gethen core renderer slice.

Run `pnpm build` first, then open `index.html` directly in a browser. The demo imports the built core package from `../../packages/core/dist/`.
