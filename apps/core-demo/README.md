# Core Demo

Framework-neutral demo for the initial Gethen core renderer slice.

Run `pnpm demo`, then open `http://127.0.0.1:4173/apps/core-demo/`.
The demo source is split across `index.html`, `styles.css`, and `src/main.ts`.
It imports the built core package through the browser import map in `index.html`.
