# Renderer Prototype

Dependency-free virtualized DOM prototype for the pre-alpha renderer research gate.

Open `index.html` directly in a browser. The prototype generates `100,000` logical rows by `50` logical columns, renders only the visible cells plus overscan, and reports basic render counters on screen.

This is research code, not production package code.
